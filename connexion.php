<?php

    $pdo = new PDO('mysql:host=localhost;dbname=catering', 'root', 'password');

    // $stmt = $pdo->query("SELECT * FROM VIP");
    // while ($row = $stmt->fetch()) {
    //     echo $row['Ctel']."<br />\n";
    // }

?>